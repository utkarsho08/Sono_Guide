# Requirements manifests

- `../requirements.txt` — lightweight runtime packages
- `ai.txt` — heavy AI packages (ultralytics)
- `optional.txt` — optional analytics/science packages
- `dev.txt` — development tooling

See `docs/DEPENDENCY_PLAN.md` for install order and launcher usage.
